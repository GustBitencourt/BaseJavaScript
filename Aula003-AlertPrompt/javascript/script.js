const num1 = Number(prompt('Digite um número'))
const num2 = Number(prompt('Digite um número'))

const resultado = num1 + num2

const alertando = alert(`O resultado é ${resultado}`)

const confirmation = confirm(`O resultado da soma é ${resultado} está correto?`)